// 先将数组分割为最小的单元,然后对数组进行排序,后合并为更大的有序数组
// 简单版本
function mergeSort(arr) {
  if (arr.length <= 1) {
    return arr;
  }
  let mid = Math.floor(arr.length / 2);
}

// 支持原地排序版本
function mergeSortOptimized(params) {}
